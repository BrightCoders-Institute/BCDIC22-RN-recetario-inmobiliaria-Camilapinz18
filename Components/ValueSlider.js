import { StyleSheet, Text, View, SafeAreaView, ScrollView } from 'react-native'

export default function ValueSlider () {
  return (
    <View>
      <Text>I am a value slider!!</Text>
    </View>
  )
}

const styles = StyleSheet.create({})
